<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Arch">
            <directory>tests/Arch</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="ALLOWED_REGISTRATION_EMAILS" value=""/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Real Postgres: PostGIS/pgvector columns cannot exist on SQLite. See docs/conventions/11-testing.md.
             Host/port/credentials are inherited from .env; only the database name is overridden. -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="travel_companion_test"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!--
            No model keys in tests. The queue is SYNC here, so a job dispatched
            from the ranking path runs inline — which means the suite would make
            real, paid Gemini calls on every run, and a test's assertions would
            depend on what a model felt like saying that afternoon.

            Empty key ⇒ GeminiClient fails fast ⇒ the caller falls back to the
            template, which is the same path production takes when the model is
            down. Tests that DO want a generation bind a FakeLlmClient explicitly.
        -->
        <env name="GEMINI_API_KEY" value=""/>
        <env name="DATATOURISME_API_KEY" value=""/>
        <env name="OPENAGENDA_API_KEY" value=""/>
    </php>
</phpunit>
